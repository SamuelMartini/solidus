<% if defined?(image) && image %>
  <%= render 'spree/shared/image', image: image, size: :product, itemprop: "image" %>
<% else %>
  <%= render 'spree/shared/image', image: @product.gallery.first, size: :product, itemprop: "image" %>
<% end %>
