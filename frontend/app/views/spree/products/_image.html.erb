<% if defined?(image) && image %>
  <%= render 'spree/shared/image', image: image, size: :product %>
<% else %>
  <%= render 'spree/shared/image', image: @product.display_image, size: :product %>
<% end %>
